{% extends 'base.html.twig' %}

{% block title %}Edition de catégorie{% endblock %}

{% block body %}
    <h1 class="text-primary fw-bold">{{category.title}}</h1>

    {{ include('back/category/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="mt-3 mb-3 col-3 btn btn-primary"  href="{{ path('app_back_category_index') }}">Retour</a>

    {{ include('back/category/_delete_form.html.twig') }}
{% endblock %}
