<form method="post" action="{{ path('app_back_allergen_delete', {'id': allergen.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet allergène ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allergen.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
