<form class="text-center" method="post" action="{{ path('app_back_recipe_delete', {'id': recipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe.id) }}">
    <button class="mt-3 col-5 btn btn-danger">Supprimer</button>
</form>
