{% extends 'base.html.twig' %}

{% block title %}Edition d'un rayon{% endblock %}

{% block body %}
    <h1 class="text-primary fw-bold">{{department.name}}</h1>

    {{ include('back/department/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="mt-3 mb-3 col-3 btn btn-primary" href="{{ path('app_back_department_index') }}">Retour</a>

    {{ include('back/department/_delete_form.html.twig') }}
{% endblock %}
