{% extends 'base.html.twig' %}

{% block title %}Edition utilisateur{% endblock %}

{% block body %}
    <h1>Editer {{user.nickname}}</h1>

    {{ include('back/user/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="mt-3 col-3 btn btn-primary" href="{{ path('app_back_user_index') }}">Retour</a>

    {{ include('back/user/_delete_form.html.twig') }}
{% endblock %}
