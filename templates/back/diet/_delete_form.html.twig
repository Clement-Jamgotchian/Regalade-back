<form method="post" action="{{ path('app_back_diet_delete', {'id': diet.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce régime?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diet.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
