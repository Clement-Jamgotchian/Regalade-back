{% extends 'base.html.twig' %}

{% block title %}Edition d'un régime{% endblock %}

{% block body %}
    <h1 class="text-primary fw-bold">{{diet.name}}</h1>

    {{ include('back/diet/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="mt-3 mb-3 col-3 btn btn-primary" href="{{ path('app_back_diet_index') }}">Retour</a>

    {{ include('back/diet/_delete_form.html.twig') }}
{% endblock %}
