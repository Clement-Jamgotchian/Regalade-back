{% extends 'base.html.twig' %}

{% block title %}Edition d'un ingrédient{% endblock %}

{% block body %}
    <h1 class="text-primary fw-bold">{{ingredient.name}}</h1>

    {{ include('back/ingredient/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="mt-3 mb-3 col-3 btn btn-primary" href="{{ path('app_back_ingredient_index') }}">Retour</a>

    {{ include('back/ingredient/_delete_form.html.twig') }}
{% endblock %}
